/*#include <stdio.h>

void stampaj(int niz[], int n) {
	int i;
	for (i = 0; i < n; i++)
	{
		printf("\n %d", niz[i] * niz[i]);
	}
}

void pomnozi(int* p){
	*p = *p * 6;
	printf("Izmenjeno ? %d", *p);
}

int main() {

	int i;
	int a[] = { 1, 2, 3 };
	int n = sizeof(a) / sizeof(int);

	pomnozi(&a[1]);

	stampaj(a, n);

	return 0;
}
*/
